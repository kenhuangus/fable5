# Coding agent -- good example

I'm working on shipping the checkout flow for a small SaaS billing team. They
need the failing build fixed so the deploy pipeline can unblock the Friday
release. With that in mind: diagnose the build failure and propose a minimal fix.

Report your findings and stop. Do not apply a fix until they ask for one.

Before reporting progress, audit each claim against a tool result from this
session.
